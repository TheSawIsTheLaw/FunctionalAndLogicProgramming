(defun findHypo (x y) (sqrt (+ (* x x) (* y y))))

(sqrt (+ (* x x) (* y y)))
    (+ (* x x) (* y y))
        (* x x)
            x вычисляется к x
            x вычисляется к x
            применяется * к x и x
            x
        (* y y)
            y вычисляется к y
            y вычисляется к y
            применяется * к y и y
            y
        применяется + к x и y
        x + y
    применяется sqrt к x + y
    x + y